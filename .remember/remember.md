# Handoff

## State
Key-figures page on `origin/v4` at `3fbf81da`. All 28 cards plus Nate Soares have `p(sentience)` and `p(doom)` under the credentials line. Numbers only when the person stated one; otherwise a word (`high`, `very high`, `moderate`, `moderate low`, `low`, `very low`, `none`, `unclear`, `unsure`, `uncommitted`, `unknown`, `non-negligible`, `non-zero`, `serious chance`). Nate is in Opposers after Yudkowsky. Portrait is `images/soares.webp` at 200×200. His take is signed — Grok. Yudkowsky's credentials now include co-author of *If Anyone Builds It, Everyone Dies*.

## Next
No open edit. Head is still `3fbf81da`. If Brad continues, he reviews the live page after Ctrl-Shift-R. Evening talk was opinion only: papers, wiki, and who the site is for.

## Context
Lowercase `p`. Do not invent percents. Nate's doom line says "kill humanity," not "us." Tegmark's `>90%` is if the race stays unregulated. Push small copy edits to `origin/v4`.
