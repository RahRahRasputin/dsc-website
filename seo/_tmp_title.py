desc = (
    "Adaptive batch sizing adjusts inference batch size in real time from GPU "
    "memory, KV cache growth, queue depth, and latency targets to balance "
    "throughput and TTFT."
)
print(len(desc), repr(desc))
desc2 = "Adaptive batch sizing adjusts inference batch size in real time from GPU memory, KV cache growth, queue depth, and latency targets—balancing throughput and TTFT."
print(len(desc2), repr(desc2))
desc3 = "Adaptive batch sizing adjusts inference batch size in real time using GPU memory, KV cache growth, queue depth, and latency targets to balance throughput and TTFT."
print(len(desc3), repr(desc3))
desc4 = "Adaptive batch sizing tunes inference batch size in real time from GPU memory, KV cache growth, queue depth, and latency targets to balance throughput and TTFT."
print(len(desc4), repr(desc4))
